version       = "0.1.0"
author        = "Dylan Vann"
description   = "A new awesome nimble package"
license       = "MIT"
srcDir        = "src"
skipDirs      = @["tests"]

requires "nim >= 1.4.2"
requires "itertools >= 0.3.0"
